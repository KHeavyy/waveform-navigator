# E2E Tests

Playwright end-to-end and visual-regression tests for `WaveformNavigator`, run
against the demo app.

Guidance for this suite — the commands, the browser projects, the spec
inventory, the demo audio it plays, and the rules for updating committed visual
snapshots — lives in [`CLAUDE.md`](../CLAUDE.md) under "Commands" and "Testing".
That file is the single source of truth; do not duplicate instructions here.
